// @NOTE: Import from libraries.
import { SkeletonDisplayText } from "@shopify/polaris";

/**
 * TagsVariants - Loading animation for `Tags` component.
 *
 * @return {JSX.Element}
 */
export default function TagsVariants() {
    return <SkeletonDisplayText size="small" />;
}
