// @NOTE: Import library functions.
// {...}

// @NOTE: Import custom functions.
// {...}

// @NOTE: Import misc.
// {...}

type Resume = {
    id: number;
    image: string;
    title: string;
    description: string;
};

export default Resume;
